# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Dependencies
vendor/
node_modules/

# Cache
*.cache

# Gebautes Auslieferungsmaterial (Install-ZIPs). Gehoert an das
# GitHub-Release, nicht in die Versionsverwaltung — unverfolgt meldet
# `git status` es sonst dauerhaft, und echte Aenderungen gehen im
# Rauschen unter.
/dist/
